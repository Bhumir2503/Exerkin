npm install
npm start
scan qr code



## Workflow
* create new branch for each issues
* if you encounter an issue, future fix, future to-do - create an issue in github


git commands
1. git pull
2. git checkout -b branchname
3. git add .
4. git commit -m "tmp"
5. git push --set-upstream origin tmp